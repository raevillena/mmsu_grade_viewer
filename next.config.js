/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Next.js 16 features - Cache Components can be enabled if needed
  // experimental: {
  //   cacheComponents: true,
  // },
};

module.exports = nextConfig;

